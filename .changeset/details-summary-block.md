---
"@k11k/better-blocks-react-renderer": minor
---

Add support for the `details` (collapsible) block. It renders a native, accessible `<details>` / `<summary>` disclosure, honors `defaultOpen` via the HTML `open` attribute, supports arbitrarily nested `details`, and can be overridden through the `blocks.details` prop.
