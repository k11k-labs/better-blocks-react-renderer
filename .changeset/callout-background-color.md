---
"@k11k/better-blocks-react-renderer": patch
---

Fix callout (admonition) blocks not showing a background color on the frontend. The default callout renderer now applies a subtle accent-tinted background, balances the vertical spacing to match GitHub's alert styling, and uses GitHub's exact Caution accent color.
